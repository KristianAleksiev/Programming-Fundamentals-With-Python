# HTTP Protocol - Concepts - Text-based client-server protocol for the Internet - HyperText Transfer Protocol\
## HTTP, TCP, IP, Ethernet

# HTTP Developer tools
# HTML Forms

## Get request - Safe operation, easy to share, doesn't change anything on the server
### Method, Headers, Body - CRUD
### Method , URL, Protocol version

## Post request - Sends data, doesn't record it in the URL, makes changes to the server

# HTTP Response
## Status code
## Content type and disposition - Content-Type: text/html; charset=utf-8 # Encodedl with UTF-8 HTML Page

# URL and URL Structure
## Uniform resource locator - Protocol, host(IP)->DNS, port, path, query string(optional data), fragment